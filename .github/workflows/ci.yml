# workflow_dispatch: GitHub Actions trigger type specifically for manual runs.
name: CI

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'pom.xml'
      - 'Dockerfile'
      - 'Makefile'
  workflow_dispatch:

jobs:
  build-test-lint-publish:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build API
        run: make build

      - name: Run tests
        run: make test

      - name: Perform code linting
        run: make lint

      - name: Docker login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker build and push
        run: |
          make docker-build IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/student-crud-api VERSION=${{ github.sha }}
          make docker-push IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/student-crud-api VERSION=${{ github.sha }}